import { useOutletContext } from "react-router-dom";

function Apply() {
    return(
        <div>
            <p>Jobs</p>
            {/* this would be a scrollable section with jobs. will have a search field as well */}
        </div>
    )
}

export default Apply;